# node.js나 react aglular는 cdn방식을 사용. j쿼리는 프론트쪽 개발하거나 할때 가끔 볼 수 잇음. 한국에서는 web사이트 제작에서는 php형식(예로 홈쇼핑)으로 제공하는 경우 많다. php-j쿼리 방식으로 자주 진행한다.
# 국가사업으로는 자바 j쿼리 리액트쪽으로 자주 사용. 요새는 ai로 인해 파이썬 자주 도입. 1월 8일즈음 리액트 적용한 ui만들기 할 것이다.
# 참조타입의 시작으로... array이라 하는 배열명이 있으며 , []는 배열의 표현이다. 내부 쉼표로 구성되어있고 순차적 구성이 되어있다. 그래서 index라는 위치값이 있다. 그리고 배열도 다양한 자료를 넣을 수 있다. string도 가능.-> 대괄호가 있다(어? 배열인가?) 그리고 쉼표가 있다? 순차적 구성이다. Ex)[2,4,6,8] ->2 ㅣ 4ㅣ6 ㅣ8의 순서로 배열되어 있다. 
# !!!!!!!!!!!  아마 면접코딩테스트용 !!!!!!!!!참조되는 자료형은 object(객체)다. 이걸 기억해놓고 참조형은 object 타입이다. 자료형과는 반대이다. 객체라는 특성을 잘 기억하자.
# 대괄호 안에 index값을 넣고 이를 기입해놓아야 위치의 값을 꺼낼 수 있다.
# const a = [] 를 설정하면  [](배열)이라는 구조는 고정되어있지만 안에 자료는 얼마든지 변경 가능하다. 따라서 배열 사용할 때에는 보통 const로 사용한다. 안정적 사용을 위해 const로 사용하자. -> object형을 다룰 때에는 const를 사용하자 그냥!!!
# const b = [1,2,3,4,5,6] 후 b = (6) [1,2,3,4,5,6] 이 출력된다. b[0] =1 I b[1]=2 I -> 그런데 하나씩 꺼내 쓰기 힘들기때문에 배열에 반복문을 사용해서 꺼낸다. -> 배열과 반복문은 같이 자주 사용된다. 

# !!!!!!!!!!! 반복문 기본 문법 for ( ; ; ) {  } !!!!!!!!!!!!!!!!!! --------->   for( let i = 0 ; i < 1 ; i = i + 1 ) { console.log("반복");}  
## ( 반복문 안에서의 변수 선언; 조건식(반복횟수 제약하기위해); 선언한 변수의 증감할 것 넣기) 가운데의 값이 참일때는 계속 진행하고 값이 거짓일 때 반복은 멈춘다. 그래서 가운데에는 false 설정할 수 있는 값을 설정. 나중에는 함수형으로 바뀌고 이게 기본형이다. 그래서 이 문장구조를 기본적으로 배워 놓자.

# 1. for (;true;) {console.log("반복");} -> 무한 반복 2.for (;false;) {console.log("반복");} -> 멈춤.
------------------------------------------------------------------------------------------------------

# 전역변수  vs 지역변수 //1. 변수 선언 및 값 설정 -전역변수 -> {}내에서도 밖에서도 사용가능. 여기서 변수 선언인데 const를 쓴다? ... 안된다.
## for(/*2.변수 선언 및 값 설정*/ ; ; ) {} =지역변수 {}내에서만 변수 사용 가능하다.

# let i = 0; for(; i < 1 ; ) { console.log("반복"); i = 1;}  console.log(i); -> 계속 1번만 반복한다.

# let i = 0; for(; i < 1 ; ) { console.log("반복"); i = i + 1;}  console.log(i); === # let i = 0; for(; i < 1 ; i = i + 1 ) { console.log("반복"); }  console.log(i); -> 계속 1번만 반복한다.

# for( let i = 0 ; i < 1 ; i = i + 1 ) { console.log("반복");}  

# const a = [1,2,3,4,5];

for( let 인덱스 = 0 ; 인덱스 < a.length ; 인덱스 = 인덱스 + 1 ) {
    // 배열의 값을 하나씩 꺼내기.
    console.log(`배열${인덱스} : ${a[인덱스]}`); }

# const a = [1,2,3,4,5];

for( const index in a ) {
    
    console.log(`배열${index} : ${a[index]}`);
}          
-> 포인이라는 반복문 in : 위치값을 꺼내주는것  -> 객체를 반복하는 배열문일때 사용. *** 그리고 지역변수  (for괄호 안에 설정하는 변수)의 경우는 let, var, const다 사용가능.

# const a = [1,2,3,4,5];

for( const value of a ) {
    
    console.log( value );
}  
-> 포오브 반복문  : 오로지 값만 알고 싶을 때 쓰는 반복문

# const a = [1,2,3,4,5];

function event(값, 인덱스){
    console.log(`배열${인덱스} : ${값}`);
    }

a.forEach(event) ;
-> 함수를 이용한 반복문.

# ================>한쪽 방향으로만 배열 ===일차원 배열 [위의 방식들이 ]

# ================>양쪽 방향으로만 배열 ===2차원 배열 =>중첩 for문(for문 2가지)

# 다시 돌아와서 for문 시작
//구구단 단* 곱값 = 결과 1 * 1
for(let i =1 ; i <= 9 ; i++ ) { 

for(let j =1 ; j <= 9 ; j++ ) {  console.log( `${i} * ${j} = ${i *j}`); }
console.log();
}

for(let i =1 ; i <= 9 ; i++ ) { 

for(let j =1 ; j <= 9 ; j++ ) {  console.log( i + "*" + j + "=" + (i*j) ); }
console.log();
}

# 전치 와 후치의 개념다시 한번 복습하기.

# for (;;) {
console.log(i);
}

# 값 += 기호; -> 값= 값 + 기호 ;

#
