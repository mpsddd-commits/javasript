# 리액트

# 노드js는 필수 설치 해야한다. vs코드도 기본. w3스쿨 사이트 리액트 탭 방문 추천. 최근에 리액트 19버젼 나옴. 18버젼도 보는것 추천. 레거시 리액트 사이트도 추천(18버젼). 리액트 네이티브 (모바일용(앱형식)) -자바스크립트 기반.
# 1.next.js는 무겁기 때문에+ 자바+타입스크립트를 기반으로 하는 것. 2.vite를 사용 권장(가벼움) - 자바스크립트기반 or 3.cra버전(18버젼)에서 나오는 것 4.기존에서 불러오기 ==> 비트가 제일 빠르게 실행 되고 좋다고 할 수 있다.
# 리액트는 타입이 모듈이 기반이다 (ES 가능하게) common js형식이 아니다!
# 퍼블릭 파일안에 index.html이 있고 대문파일이다 할 수 있다. 
# 인덱스 html안에 바디에서 root라 쓰여있는 것은 도큐먼트에서 볼 수 있다. <src.index.js>에서 const root = ReactDOM.createRoot(document.getElementById('root'));를 사용
# jsx는 자바스크립트에서 처음으로 <>(태그형식)으로 사용가능하게 만들었다. css도 사용가능하다. jsx를 쓴다 -> 리액트를 사용한다 라고 생각하면 된다.
# jsx에서는 className으로 태그를 설정하는데 왜냐하면 javascript에서 이미 class라는 태그가 있기 때문에 바꿔 썼다. 
# 리액트(jsx)에서 변수 대입은 {}로 표현식을 사용한다. node.js에서 $  쓰는것과 대비된다.
# 리액트에서 문법 오류가 나면 무조건 브라우져이도 표현이 된다. 그리고 문법이 깐깐하다. return()에서 소괄호 안에 무조건 1개의 태그는 있어야한다. ex)<div></div> 그리고 무조건 1개의 태그로 구성되어야지 여러 태그를 만들면 안된다. 그렇게 하고 싶다면 하나의 큰 부모 태그<></>(프레그먼트)를 만들고 시작해라.
# 렌더에도()형식으로 구성 되어 있기 때문에 무조건 부모 태그 하나가 존재해야하며 여러개는 안된다. ( 여기는 jsx영역이고 html형식으로 javascript 구성이 가능하다. ) -소괄호 안 ||||||||||| 그리고 밖에서 const txt = "안녕하세요";로 만들고 jsx영역 안쪽에 {}를 포함해서 만들면 된다. |||**주의 사항 react.dom형식을 거친 부분에서만 가능하다.
# react.strictmode => 개발때만 동작하며 잘 되는지 안되는지 감지하는 모드 -> 콘솔로 진행되는게 한 번 더 진행되는 모드 + 문법 조사등을 실행 렌더시에 사용하는데 대소문자확인해서 태그인지 함수인지 확인케 하기위해.  ***개발 단계에서 두 번 출력이 생기면 이걸로 주석으로 확인해보고 해봐라. (실무에서 제일 많이 겪는 상황)   || 개발단계가 끝나면 프래그먼트로 변경되어 껍데기 표시가 된다. 그리고 최종적으로 배포가 되면 주석으로 제거된다. 
# 리액트에서는 자바스크립트와 다르게 호출이 자동으로 될 수 있다 , jsx영역 내부에서 나오는 것은 함수 호출 코드라 생각하면 된다. 
# 리액트에서는 하나의 법칙이 생긴다 |||||||||||||  jsx를 리턴하는 함수는 첫번째 문자에 무조건 대문자로 쓴다. 왜냐하면 html태그와 헷갈리지 않게 하기 위해 - html태그는 무조건 소문자 이니까.  ==>그렇다고 모든 함수에 첫문자 대문자 쓰지말고 그냥 쓰는 함수는 소문자로 써라. 리액트 쓰는 함수에만 첫 문자 대문자 써라. export는 소문자 써도 되긴하다. 그렇다고 소문자쓰는거 하지마라.
# 리턴문의 경우 고정값이기 때문에 밖에 따로 함수 선언해서 넣는 것도 가능하고 조건문이나 반복문의 경우 함수 내부(jsx영역)에서 진행해야한다.
# 함수를 만들었을 때 그 값이 너무 커서 하나의 함수로 선언 후 리턴 안에 넣을 때 쓰는 함수를 컴포넌트라 한다.
# ?는 ? 앞의 내용이 맞으면 실행 아니라면 실행하지 마라. ==> 밑에 빨간줄 끄여있을때 모르겠으면 ?를 사용하라~~~ 그리고 모르겠으면 펑션 안에 중괄호 안에 두가지 변수를 콘솔로그로 확인해라. 
# 비트에서 퍼블릭 폴더는 정적파일들을 올려놓는 곳이라 생각해라. 동적파일은 퍼블릭이 아닌 src폴더에 있어야한다. 이미지는 보톤 백엔드에서 가져온다. ==> 아이콘 정도만 퍼블릭이나 src폴더에 쓰지 이미지는 풀스택과정에서는 여기에 넣어놓지 않는다.
# css를 app쪽에서 사용하게 되면 개발자모드에서 css에서는 보이지 않는다 -> 왜냐하면 자바스크립트 내부에서 사용 및 동작하기 때문이다. -> js항목에 있다.  f12로 보고 싶으면 index.html에 넣어라 그거 아니면 보통 jsx안에 import한다. 이미지의 경우는 보인다. =>app.jsx를 보면 안다.
# 리액트를 시작할 때 app.jsx부터 시작한다고 생각하면 된다. 그리고 app.css파일도 지우거나 주석 처리 
# 이렇게 해서 홈페이지 만드는 것을 5번 정도 혼자 숙달 시켜 보면 감이 잡힐 것이다.
# 그리고 웬만하면 화살표 함수 형식으로 만들어 사용하는 것을 추천한다. + !!함수든 변수든 지역관리 잘하기.- 첫 습관을 잘 잡아보자
# 오늘 배운 부분에서 page3의 경우는 콜백함수 위치에 있기 때문에 jsx내에 화면 호출용 함수가 아니므로 대문자할 필요 없이 그냥 진행하면 된다.
# 리액트에서 ? 사용은 최소한으로 하는 것이 좋다. 그리고 보통 반복문의 경우 자주 사용한다.
# 컴포넌트의 명명법?
# jsx구조에서 나오는 값이기 때문에 obj라도 화면 출력이 가능하다. 단순 obj형식은 사용이 되지 않는다.({} 이런거) jsx에 사용하는 밸류값이라 가능했다.
# 문서형식의 DOM 형태로 만드는 방법이 리엑트에서 제일 중요하다.
# 파일 분리할때 import, export 잘 구분 해서 사용하고 메인의 경우 export default 로 사용하고 나머지 짜잘한것들은 {}안에 표기. =>그래서 파일들이 많이 생기기 때문에 파일 관리를 잘 할 수 있게 하자.
# 
