# 리액트 라우더에서 3번째 인스톨 사용하기
# browserRouter -브라우져에 라우터를 사용할 것이다 라는 태그 ,, <<<Routes- 라우트 묶음 ,, Route를 하위에 모아 놓은 것 >>>> ==> 라우트즈와 라우트는 한 묶음으로 브라우져 라우트 안에 여러번 사용 할 수 있다.

# 기본형 return( <BrowserRouter> 내부 <Routes> 내부  <Route path="/" element={<Page1/>} /> 뒤에 </루트즈> </브라우져루트> 이런 식으로 진행 . 그리고 오늘 메인에 올림.

# 리액트 부트스트랩 적용할때 명령어와 jsx내부에 임포트 삽입하는데 app.jsx에 넣는다 왜냐하면 루트기반만들어지는 시작점이 app이기 때문에 

# 리엑트에서 오브젝트에는{} 키와 밸류가 잇어야한다.

# 리액트 훅  => useeffect(화면에 데이터를 사용하여 채우기) , usestate(다른페이지에서 보내주는 데이터받기)    || 라우터 훅  => useNavigate, useLocation

# switch의 경우 숫자로 배열하고 break를 써서 범위를 지정하는것이 좋다 === if문과 유사하다고 할 수 있다.

# 달력을 제작한다 할때 for문으로 31일을 계산해서 7일치를 스위치를 써서 사용해서 날짜를 설정하면 관리하기 편하다.

# 함수 선언문, 함수표현식의 차이 위 식들보다 위에 함수 호출을 하혐 함수 선언문은 가능하고 함수표현식은 안된다. 왜냐하면 함수표현식으로 해놓고 그보다 위에 함수 호출을 하면 함수로 인식하지 않고 변수로 인식하기 때문이다.
